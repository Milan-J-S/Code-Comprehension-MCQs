//Check if a string is equal to its reverse
int isPalindrome(char *s) {
  int n = strlen(s);
  int i = 0;
  int j = n-1;
  while (i < j) {
    if (s[i] != s[j]) {
      return 0;
    }
    i++;
    j--;
  }
  return 1;
}