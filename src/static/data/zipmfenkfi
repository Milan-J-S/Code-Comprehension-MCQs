// Check if two arrays contain a common integer
int commonUnsorted(int n, int A[n], int m, int B[m]) {
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			if (A[i] == B[j]) {
				return 1;
			}
		}
	}
	return 0;
}